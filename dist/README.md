# Distribution Config Files

This directory contains configuration and data files used during distribution builds.

The `plat` directory contains per-platform config files. Files in the root are
intended for all platforms.
